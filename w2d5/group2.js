// matrix search

// Bryce

// given two matrices, return true or false if the second matrix is contained within the first.
// a matrix is just an array of arrays
// we can assume our matrices are rectangular

// traverse matrix1 and find a match from matrix2[0][0]
// after finding a match, increment

function matrixSearch(matrix1, matrix2) {
    for (var i = 0; i < matrix1.length; i++) {
        for (var j = 0; j < matrix1[0].length - 1; j++) {

            if (matrix1[i][j] == matrix2[i][i]) {
                temp1 = j + 1
                if (matrix1[i][temp] == matrix2[i][temp]) {

                }

            }

            var k = 0;
            var h = 0;
            if (matrix1[i][j] == matrix2[k][h]) {

            }

        }
    }
}

// while (matrix1[i][j])

//     let x = [
//     ['a', 'b', 'c'], // 0
//     ['a', 'b', 'c'], // 1
//     ['a', 'b', 'f'], // 2
//     ['a', 'c', 'x'] // 3
// ];

// the first element is the y axis
// the second element is the x axis
console.log(x[0][0])

let y = [
    ['b', 'c'],
    ['b', 'c']
]


let z = [
    ['a', 'c'],
    ['a', 'c']
];
m orvajmatri! //ari ()i[j] {}
    va // // // // // // //matrix1[i][j] == matrix2[k][h]( 1i